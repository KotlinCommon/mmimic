version: '3.8'

services:
  discord-bot:
    container_name: 'mimic-st-bot'
    environment:
      - TOKEN_DISCORD=${TOKEN_DISCORD}
    build: .
    ports:
      - "80:8080"
      - "443:8443"
    networks:
      - mimic-st-network
networks:
  mimic-st-network:
    name: "mimic-st-network"